package com.jesuslcorominas.teamflowmanager.ui.theme

import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

object TFMTypographyStyles {
    val headingLHighlight =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Bold,
            fontSize = 40.sp,
            lineHeight = 48.sp,
        )
    val headingL =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Normal,
            fontSize = 40.sp,
            lineHeight = 48.sp,
        )

    val headingMHighlight =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Bold,
            fontSize = 32.sp,
            lineHeight = 40.sp,
        )

    val headingM =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Normal,
            fontSize = 32.sp,
            lineHeight = 40.sp,
        )

    val headingSHighlight =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.SemiBold,
            fontSize = 24.sp,
            lineHeight = 28.sp,
        )

    val headingS =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Light,
            fontSize = 24.sp,
            lineHeight = 28.sp,
        )

    val headingXsHighlight =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.SemiBold,
            fontSize = 20.sp,
            lineHeight = 24.sp,
        )

    val headingXs =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Light,
            fontSize = 20.sp,
            lineHeight = 24.sp,
        )

    val bodyLHighlight =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.SemiBold,
            fontSize = 16.sp,
            lineHeight = 20.sp,
        )

    val bodyL =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Light,
            fontSize = 16.sp,
            lineHeight = 20.sp,
        )

    val bodyMHighlight =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.SemiBold,
            fontSize = 14.sp,
            lineHeight = 16.sp,
        )

    val bodyM =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Light,
            fontSize = 14.sp,
            lineHeight = 16.sp,
        )

    val bodySHighlight =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.SemiBold,
            fontSize = 12.sp,
            lineHeight = 14.sp,
        )

    val bodyS =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Light,
            fontSize = 12.sp,
            lineHeight = 14.sp,
        )

    val bodyXsHighlight =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.SemiBold,
            fontSize = 10.sp,
            lineHeight = 12.sp,
        )

    val bodyXs =
        TextStyle(
            fontFamily = PublicSansFontFamily,
            fontWeight = FontWeight.Light,
            fontSize = 10.sp,
            lineHeight = 12.sp,
        )
}
